<ul class="nav-list">
    <li class="nav-item">
        <a href="@Url.Action("Index", "Config")" class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "active" : "")">
            <i class="icon-overview"></i>
            配置总览
        </a>
    </li>
    <li class="nav-item">
        <a href="@Url.Action("PLCConfig", "Config")" class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "PLCConfig" ? "active" : "")">
            <i class="icon-plc"></i>
            PLC配置
        </a>
    </li>
    <li class="nav-item">
        <a href="@Url.Action("EquipmentConfig", "Config")" class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "EquipmentConfig" ? "active" : "")">
            <i class="icon-equipment"></i>
            设备配置
        </a>
    </li>
</ul>

<style>
.nav-list {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 20px;
}

.nav-item .nav-link {
    display: flex;
    align-items: center;
    padding: 8px 16px;
    text-decoration: none;
    color: #606266;
    border-radius: 4px;
    transition: all 0.3s;
}

.nav-item .nav-link:hover,
.nav-item .nav-link.active {
    color: #409eff;
    background-color: rgba(64, 158, 255, 0.1);
}

.nav-item .nav-link i {
    margin-right: 8px;
    font-size: 16px;
}

.icon-overview::before { content: "🏠"; }
.icon-plc::before { content: "🔌"; }
.icon-equipment::before { content: "⚙️"; }
</style>